{
  "name": "chart-rabbit",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/frontend"
  ],
  "scripts": {
    "build": "npm install --prefix apps/frontend && npm run build --prefix apps/frontend",
    "dev": "npm run dev --prefix apps/frontend"
  }
}
